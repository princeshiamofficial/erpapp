---
description: Managing Genkit AI flows and development.
---

1. Start Genkit UI for testing and debugging flows:
// turbo
   ```bash
   npx genkit ui
   ```

// turbo
2. Update AI flows in `src/ai/dev.ts`.
3. Use the `genkit:watch` script for faster iterations during flow development:
   ```bash
   npm run genkit:watch
   ```
